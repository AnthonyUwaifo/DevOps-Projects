pipeline {
    agent { 
        node {
            label 'docker-agent-alpine'
            }
      }
    triggers {
        pollSCM '* * * * *'
    }
    stages {
        stage('Build') {
            steps {
                echo "Building..."
                sh '''
                touch /var/jenkins_home/requirements.txt
                echo "Created this requirement to test this automated pipeline" > requirements.txt
                '''
            }
        }
        stage('Test') {
            steps {
                echo "Testing..."
                sh '''
                touch /var/jenkins_home/deliverables.txt
                echo "Created this deliverable to test this automated pipeline" > deliverables.txt
                '''
            }
        }
        stage('Delivery') {
            steps {
                echo 'Delivery...'
                sh '''
                touch /var/jenkins_home/logs.txt
                echo "Created this log to say 'Build was successful'" > logs.txt
                '''
            }
        }
    }
}